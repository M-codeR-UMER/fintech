APP_ENV=development
ALLOW_SEED_RESET=true
# Optional: if set, POST endpoints require this token in x-api-token header
# API_TOKEN=replace-with-strong-token
# Optional custom DB path
# DB_PATH=backend/finpay.db
